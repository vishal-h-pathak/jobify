__pycache__/
*.py[cod]
*$py.class
*.so
.Python

.pytest_cache/
.ruff_cache/
.mypy_cache/

.venv/
venv/
env/
.env
.env.local

# The active user's generated profile (real PII: name, email, location,
# CV, etc.). Onboarding writes <repo_root>/profile/; the loader prefers it
# over the shipped neutral baseline profile.example/. NEVER commit it.
/profile/

*.egg-info/
dist/
build/

.DS_Store
.idea/
.vscode/

graphify-out/
.graphify_*.json
.graphify_python
.graphifyignore
obsidian/
cache/

# Forensic backup of .git/ from a prior secret-removal pass. 2.2 MB,
# kept on local disk only — never commit.
.git.backup-pre-secret-cleanup/

# jobify hosted env (Cowork H7 Part-B prep, 2026-07-03)
.env.*

# subagent-driven-development scratch workspace (task briefs, reports,
# review packages) — never commit.
.superpowers/
