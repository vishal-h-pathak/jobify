<!-- learned-insights.md — GOLDEN ONBOARDING EXAMPLE (Sam Rivera). Ships
     intentionally near-empty. -->
# Sam Rivera — Learned Insights

USER-LAYER. This file starts empty and accumulates generalizable preferences
over time (e.g. "consistently skips roles where accessibility is unfunded"). It
is loaded AFTER profile.yml + cv.md, so an insight here overrides earlier
statements when they conflict.

In this single-user tool it is maintained by hand if at all — it loads fine
empty.
