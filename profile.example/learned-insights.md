<!-- learned-insights.md — EXAMPLE persona. Ships intentionally near-empty. -->
# Alex Quinn — Learned Insights

USER-LAYER. This file starts empty and is meant to accumulate generalizable
preferences over time (e.g. "consistently skips roles that are on-call heavy
without comp adjustment"). It is loaded AFTER profile.yml + cv.md so any
insight here overrides earlier statements when they conflict.

In this single-user tool the dashboard's automatic writer is trimmed, so you
maintain this file by hand if you want it — or leave it empty. It loads fine
empty.
