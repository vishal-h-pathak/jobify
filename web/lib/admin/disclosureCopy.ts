/**
 * Session 29 (ONB-D) owner decision #2: onboarding's first screen needs one
 * quiet line of consent copy. Exported here (rather than written directly
 * into the onboarding UI) because that UI is a different session's
 * ownership — hand this string to it via the merge report.
 */
export const DISCLOSURE_COPY =
  "During the beta, the operator can review what's captured here to improve matching.";
